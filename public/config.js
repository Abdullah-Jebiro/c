// Configure the API base URL for production hosting (e.g., GitHub Pages + external backend)
// - Leave empty ('') to use same-origin (works when served by the Node server locally)
// - Set to your deployed backend origin when hosting static files (e.g., 'https://your-backend.example.com')
// - You can also override at runtime via URL param ?api=https://your-backend.example.com
window.API_BASE = '';
